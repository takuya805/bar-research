<% if i==1 %>
  <% if review.user.followed_by?(current_user) %>
    <%= link_to "フォロー済み", user_relationships_path(user_id: review.user.id, review_id: review.id), remote: true, method: :delete %>
  <% else %>
    <%= link_to "フォローする", user_relationships_path(user_id: review.user.id, review_id: review.id), remote: true, method: :post %>
  <% end %>
<% elsif i==2 %>
  <% if review.user.followed_by?(current_user) %>
    <%= link_to "フォロー済み", user_relationships_path(user_id: review.user.id, review_id: review.id), remote: true, method: :delete %>
    <%= link_to 'チャットをする', chat_path(review.user.id), "data-turbolinks":false %>
  <% else %>
    <%= link_to "フォローする", user_relationships_path(user_id: review.user.id, review_id: review.id), remote: true, method: :post %>
  <% end %>
<% elsif i==3 %>
  <% if review.user.followed_by?(current_user) %>
    <%= link_to "フォロー済み", user_relationships_path(user_id: review.user.id, review_id: review.id), remote: true, method: :delete %>
    <%= link_to 'チャットをする', chat_path(review.user.id), "data-turbolinks":false %>
  <% else %>
    <%= link_to "フォローする", user_relationships_path(user_id: review.user.id, review_id: review.id), remote: true, method: :post %>
  <% end %>
<% end %>
