<div class=mypage>
  <%= render 'user/users/sidebar', user: @user %>
<main class="bookmarks">
    <h3>お気に入りレビュー</h3>
  <div class="row">
      <% if @favorites.present? %>
      <% @favorites.each do |favorite| %>
      <div class="col-md-4 favorite-review">
        <% if favorite.review.image.present? %>
         <div><%= attachment_image_tag(favorite.review, :image, size: '150x120') %></div>
        <% end %>
        <div class="favorites">Nickname</div>
          <div><%= favorite.review.user.name %>
             <%= link_to user_path(favorite.user) do %>
               <i class="fas fa-external-link-alt" style="color:#6600FF;"></i>
             <% end %>
          </div>
          <div class="favorites">Bar</div>
         <div><%= favorite.review.shop.name %>
            <%= link_to shop_path(favorite.review.shop) do %>
             <i class="fas fa-external-link-alt" style="color:#6600FF;"></i></div>
            <% end %>
        <div class="favorites">Title</div><div><%= favorite.review.title %></div>
        <div class="favorites">Content</div><div><%= favorite.review.post %></div>
        <% review = favorite.review %>
         <div id="favorites_buttons_<%= review.id %>" style="margin-top: 10px;">
          <%= render partial: 'user/favorites/favorite', locals: {review: review} %>
        </div>
      </div>
      <% end %>
      <% else %>
      <p　style="margin: 0 auto; margin-top:70px;">まだお気に入りレビューはありません<br >レビューを見てお気に入り登録をしてみよう！！</p>
      <% end %>
  </div>
   <div class="row">
            <div class="col">
              <div>
               <%= paginate @favorites, style: "color:white;" %>
              </div>
            </div>
          </div>
</main>
</div>